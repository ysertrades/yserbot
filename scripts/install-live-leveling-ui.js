#!/usr/bin/env node
'use strict';
const fs = require('fs');
const path = require('path');
const zlib = require('zlib');
console.log('Run from repo after pulling - content will be filled');
process.exit(1);

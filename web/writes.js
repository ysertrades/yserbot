'use strict';

/**
 * web/writes.js — RESTORED. See previous commit for full history.
 * EMERGENCY minimal restore so bot can boot; full file follows in next commit if truncated.
 */
const { EmbedBuilder, PermissionFlagsBits } = require('discord.js');
const { readJson, writeJson } = require('../utils/jsonStorage');
module.exports = { apply: async () => ({ error: 'writes_incomplete_restore' }), OPS: {} };
